<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignupActivity"
            android:label="SignupActivity" />
        <activity
            android:name=".WriteActivity"
            android:label="WriteActivity" />
        <activity
            android:name=".UserActivity"
            android:label="UserActivity" />
        <activity
            android:name=".AdminActivity"
            android:label="AdminActivity" />
        <activity
            android:name=".CounselorActivity"
            android:label="CounselorActivity" />
        <activity
            android:name=".UserMyPage"
            android:label="UserMyPage" />
        <activity
            android:name=".ReplyActivity"
            android:label = "ReplyActivity"/>
        <activity android:name=".ShowDetail"
            android:label="ShowDetail"/>
    </application>

</manifest>